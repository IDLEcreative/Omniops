/**
 * Tests for Structured Logger - Main Importer
 *
 * This file imports and runs all logger test suites.
 * Individual suites are split into separate files for maintainability.
 */

import './logger.levels.test';
import './logger.redaction.test';
import './logger.context.test';
